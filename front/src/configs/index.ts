import locales from "@/configs/locales";
import menu from "@/configs/menu"
import stores from "@/configs/stores";

export default {
    product: {
        name: "EasyEn",
        version: "v1.0.0"
    },
    menu,
    locales,
    stores
}