<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easyen.easyenglish.mapper.UserMapper">

    <insert id="insertUser" parameterType="java.lang.String">
        insert into users (username, password, email, other_info, account_lock_status, errors_count)
        VALUES(#{username}, #{password}, #{email}, #{other_info}, false, 0)
    </insert>

    <select id="getUserByEmail" resultType="com.easyen.easyenglish.entity.User">
        select * from users where email = #{email}
    </select>

    <update id="updateUser" parameterType="com.easyen.easyenglish.entity.User">
        update users set
                         username = #{username},
                         password = #{password},
                         email = #{email},
                         other_info = #{other_info},
                         account_lock_status= #{account_lock_status},
                         errors_count = #{errorsCount}
        where user_id = #{user_id}
    </update>
    <select id="getUserById" resultType="com.easyen.easyenglish.entity.User">
        select * from users where user_id = #{user_id}
    </select>

</mapper>